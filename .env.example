# =============================================================================
# DEEP RESEARCH CONFIG (.env)
# -----------------------------------------------------------------------------
# Set the defaults or override them as needed. Comment out keys you're not using.
# =============================================================================

# Default service if user doesn't provide --service (e.g. "openai", "deepseek", "gemini").
DEFAULT_SERVICE="gemini"

# -----------------------------------------------------------------------------
# OpenAI
# -----------------------------------------------------------------------------
# Required unless you're only using DeepSeek or another OpenAI-compliant API.
OPENAI_API_KEY=""
# OPENAI_MODEL="o3-mini"
OPENAI_MODEL="gpt-4.1-mini"

# -----------------------------------------------------------------------------
# DeepSeek
# -----------------------------------------------------------------------------
# Uncomment and set if you want to use DeepSeek.
DEEPSEEK_API_KEY="your_deepseek_api_key_here"
DEEPSEEK_MODEL="deepseek-reasoner"

# -----------------------------------------------------------------------------
# Gemini
# -----------------------------------------------------------------------------
# Uncomment and set if you want to use Gemini.
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.0-flash"

# -----------------------------------------------------------------------------
# ollama
# -----------------------------------------------------------------------------
OLLAMA_HOST_ENDPOINT="http://localhost:11434/v1"
OLLAMA_MODEL="llama3.2"

# -----------------------------------------------------------------------------
# Third-party OpenAI-compliant API endpoint
# -----------------------------------------------------------------------------
# Uncomment if you want to use a service that mimics OpenAI's API (e.g., OpenRouter or Gemini).
# OPENAI_ENDPOINT="http://localhost:1234/v1"

# Default scraper to use (options: firecrawl, playwright_ddgs, playwright_serper)
DEFAULT_SCRAPER="playwright_serper"

# -----------------------------------------------------------------------------
# Firecrawl
# -----------------------------------------------------------------------------
# Always required.
FIRECRAWL_API_KEY=""

# If self-hosting Firecrawl or overriding the default URL:
# FIRECRAWL_BASE_URL="http://localhost:3002"

# -----------------------------------------------------------------------------
# Serper.dev
# -----------------------------------------------------------------------------
# Required if using playwright_serper
SERPER_API_KEY=""
